services:
  mariadb:
    image: mariadb:11.4
    container_name: mariadb_db
    environment:
      # Данные берутся напрямую из .env файла
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD}
      MARIADB_DATABASE: ${MARIADB_DATABASE}
      MARIADB_USER: ${MARIADB_USER}
      MARIADB_PASSWORD: ${MARIADB_PASSWORD}
    # ports:
    #   - "3306:3306"
    volumes:
      - ./mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - spring_network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spring_app
    environment:
      # Передаем переменные внутрь Spring Boot контейнера
      SPRING_DATASOURCE_URL: jdbc:mariadb://mariadb:3306/${MARIADB_DATABASE}?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: ${MARIADB_USER}
      SPRING_DATASOURCE_PASSWORD: ${MARIADB_PASSWORD}
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      SPRING_WEB_RESOURCES_STATIC_LOCATIONS: file:/app/static/
      SPRING_THYMELEAF_PREFIX: file:/app/templates/
    volumes:
      - ./src/main/resources/static:/app/static
      - ./src/main/resources/templates:/app/templates
    depends_on:
      mariadb:
        condition: service_healthy
    networks:
      - spring_network

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: nginx_proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/cert.crt:/etc/nginx/cert.crt:ro
      - ./nginx/cert.key:/etc/nginx/cert.key:ro
      - certbot-www:/var/www/certbot
    depends_on:
      - app
    networks:
      - spring_network

  pma:
    image: phpmyadmin/phpmyadmin:latest
    container_name: phpmyadmin
    environment:
      PMA_HOST: mariadb
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD}
      UPLOAD_LIMIT: 256M
      PMA_ABSOLUTE_URI: https://jvm.ee/pma/
    depends_on:
      - mariadb
    networks:
      - spring_network

networks:
  spring_network:

volumes:
  certbot-www:
